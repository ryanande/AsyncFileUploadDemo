@{
    ViewBag.Title = "Home Page";
}


<div class="row">
    <div class="col-md-6">
        <h1>File Upload Demo</h1>
        <p>
            Testing the default behavior of Multi File Async Uploads with HTML and Web API
        </p>
    </div>
</div>
<div class="row">
    <div class="col-md-6">

        <form role="form" action="/api/upload" method="post" enctype="multipart/form-data">
            <div class="row">
                <div class="form-group">
                    <label for="exampleInputFile">Select a File to Upload</label>
                    <input type="file" name="fileToUpload" id="fileToUpload" multiple="multiple" data-bind="event: { change: fileSelected($element.files) }" />
                </div>
            </div>
            <div class="row">
                <button type="button" id="uploadBtn" class="btn btn-primary" data-bind="click: upload">Upload</button>
            </div>
        </form>
    </div>
    <div class="col-md-6" data-bind="foreach: { data: files, beforeRemove: remove, afterAdd: add }">
        <div class="alert alert-info">
            <button type="button" class="close" data-bind="click: $parent.removeFile">&times;</button>
            <div>
                <span data-bind="text: name"></span>
                (<span data-bind="text: size"></span>
                [<span data-bind="text: type"></span>])
            </div>
            <div class="progress">
                <div class="progress-bar progress-bar-info" role="progressbar" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100" data-bind="style: { width: progress() + '%' }">
                    <span class="sr-only" data-bind="text: progress"></span>
                </div>
            </div>
            <span class="label label-dange" data-bind="text: error, visible: isError"></span>
            <span class="label label-success" data-bind="visible: isComplete">Upload complete!</span>
        </div>
    </div>
</div>

